<%= form_for(@publish_status) do |f| %>
  <% if @publish_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@publish_status.errors.count, "error") %> prohibited this publish_status from being saved:</h2>

      <ul>
      <% @publish_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :status_name %><br>
    <%= f.text_field :status_name %>
  </div>
  <div class="field">
    <%= f.label :ordering %><br>
    <%= f.number_field :ordering %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
